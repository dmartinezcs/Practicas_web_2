'use client';
import Link from "next/link";
import { useRouter } from "next/navigation";

const CountryPage = () => {
    const router = useRouter();
    return (
      <div>
        <h1>Esta página no contiene nada</h1>
      </div>
    );
};

export default CountryPage;